using System;
using TaleWorlds.CampaignSystem;
using TOW_Core.Utilities;

namespace TOW_Core.CampaignMode
{
    /*public class CampaignToBattleConnector : CampaignBehaviorBase
    {
        public override void RegisterEvents()
        {
            CampaignEvents.BattleStarted.AddNonSerializedListener(this,OnBattleStarted);
        }

        public override void SyncData(IDataStore dataStore)
        {
            throw new System.NotImplementedException();
        }
        
        
        /*
        private void OnBattleStarted(
            PartyBase attackerParty,
            PartyBase defenderParty,
            object subject,
            bool showNotification)
        {
           /// WorldMapAttribute defenderPartyWorldMapAttribute= AttributeSystemManager.GetInstance().GetAttribute(defenderParty.Id);
            TOWCommon.Say(attackerParty.Id+  " is fighting now " + defenderParty.Id);
        }
        #1#



        private void OnBattleStarte3()
        {
            
        }
    }*/
}